import { Routes } from '@angular/router';
import { HomeComponent } from './home/home'; // Tu archivo home.ts corto

export const routes: Routes = [
  // Esta es TU ruta oficial para la Tiendita de Don Pepe:
  { path: '', component: HomeComponent }, 
];